[
  {
    "task": "Write a regular expression to validate an AWS S3 bucket name. Bucket names must be between 3 and 63 characters, contain only lowercase letters, numbers, hyphens, and periods, cannot start or end with a hyphen or period, and cannot contain consecutive periods."
  },
  {
    "task": "Create a Python function that takes an AWS IAM ARN string as input and returns a dictionary with extracted components: partition, service, region, account_id, and resource. Example ARN: 'arn:aws:iam::123456789012:user/Development/product_1201/*'"
  },
  {
    "task": "Generate a JSON CloudFormation template snippet for an AWS Lambda execution role that grants basic Lambda execution permissions and allows writing logs to CloudWatch."
  }
]